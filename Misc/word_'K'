#  word:"K"

n=5
for row in range(n):
    for col in range(n):
        if col==0 or (row==n//2 and col<=n//2) or (col+row==n-1 and col>n//2) or (col==row and row>=n//2):
            print('*',end=' ')
        else:
            print(' ',end=' ')
    print()
